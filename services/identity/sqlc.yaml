version: "2"

sql:
  - engine: postgresql
    schema: ./migrations
    queries: ./queries
    gen:
      go:
        package: gen
        out: internal/adapters/outbound/postgres/gen
        sql_package: pgx/v5
        emit_interface: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: uuid
            go_type: github.com/google/uuid.UUID
          - db_type: uuid
            nullable: true
            go_type:
              import: github.com/google/uuid
              type: UUID
              pointer: true
          # pgx/v5 defaults timestamptz to pgtype.Timestamptz for both null and
          # non-null; map to time.Time / *time.Time so the repo mapping layer
          # never touches pgtype.
          - db_type: timestamptz
            go_type: time.Time
          - db_type: timestamptz
            nullable: true
            go_type:
              import: time
              type: Time
              pointer: true
